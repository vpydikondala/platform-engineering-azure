apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: team-b-ingress
spec:
  ingressClassName: nginx
  rules:
  - host: team-b.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: team-b-app
            port:
              number: 80
